DTT_URL=https://dttguide.nbtc.go.th/BcsEpgDataServices/BcsEpgDataController/getProgramDataWeb

# GSHEET_URL: deploy webdashboard/App.gs as a Google Apps Script Web App bound to your
# Google Sheet (Deploy > New deployment > Web app), then paste the deployment URL
# (the one ending in /exec) below.
GSHEET_URL=

TZ=Asia/Bangkok
REQUEST_TIMEOUT=60
APPS_SCRIPT_TIMEOUT=300
MAX_RETRIES=5
RETRY_WAIT=5
